{
  "name": "vitest-test-runner-test",
  "version": "1.0.0",
  "scripts": {
    "test": "vitest",
    "test:callback:valid": "vitest --testNamePattern=\"^ callback version valid$\"",
    "test:valid": "vitest --testNamePattern=\"valid$\""
  },
  "devDependencies": {
    "vitest": "^0.34.6"
  }
}
